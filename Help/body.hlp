\DOC body

\TYPE {body : term -> term}

\SYNOPSIS
Returns the body of an abstraction.

\DESCRIBE
{body `\var. t`} returns {`t`}.

\FAILURE
Fails unless the term is an abstraction.

\EXAMPLE
{
  # body `\x. x + 1`;;
  val it : term = `x + 1`
}

\SEEALSO
bndvar, dest_abs.

\ENDDOC
