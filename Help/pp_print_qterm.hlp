\DOC pp_print_qterm

\TYPE {pp_print_qterm : formatter -> term -> unit}

\SYNOPSIS
Prints a term with surrounding quotes to formatter.

\DESCRIBE
The call {pp_print_term fmt tm} prints the usual textual representation of the
term {tm} to the formatter {fmt}, in the form {`tm`}.

\FAILURE
Should never fail unless the formatter does.

\COMMENTS
The usual case where the formatter is the standard output is {print_qterm}.

\SEEALSO
pp_print_term, print_qterm, print_term.

\ENDDOC
