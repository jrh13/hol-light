\DOC pow2

\TYPE {pow2 : int -> num}

\SYNOPSIS
Returns power of 2 as unlimited-size integer.

\DESCRIBE
When applied to an integer {n} (type {int}), {pow2} returns $2^n$ as an
unlimited-precision integer (type {num}). The argument may be negative.

\FAILURE
Never fails.

\EXAMPLE
{
  # pow2(-2);;
  val it : num = 1/4
  # pow2(64);;
  val it : num = 18446744073709551616
}

\SEEALSO
pow10.

\ENDDOC
