\DOC is_list

\TYPE {is_list : term -> bool}

\SYNOPSIS
Tests a term to see if it is a list.

\DESCRIBE
{is_list} returns {true} of a term representing a list. Otherwise it returns
{false}.

\FAILURE
Never fails.

\SEEALSO
dest_cons, dest_list, is_cons, mk_cons, mk_list.

\ENDDOC
